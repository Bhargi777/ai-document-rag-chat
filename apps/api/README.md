# API Service

The FastAPI backend provides REST endpoints for authentication, document ingestion, retrieval, and chat sessions.

## Migrations

Use Alembic inside `apps/api` to manage schema migrations.
